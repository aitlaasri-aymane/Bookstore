{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="d-flex justify-content-center" id="homePageTitle">
    <h1 class="text-light">Welcome to our BookStore! 📚</h1>
</div>

<div id="lastLivres">
    <h1 align="center" class="text-dark">Les 10 derniers Livres 📖</h1>
    <div class="container">
      <table class="table table-striped table-hover">
        <tbody>
          <tr>
           {% for livre in livres %}
            <td>
              <a href="{{ path('livre_show', {'id': livre.id}) }}" class="text-decoration-none"><h2><i class="fas fa-bookmark"></i> {{livre.titre}}</h2></a>
                <ul class="list-inline mb-0">
                <li class="list-inline-item">
                <p class="text-muted">
                  <i class="fas fa-feather-alt"></i> Ecrit par :</li>
                    {% for auteur in livre.auteurs %}
                      <li class="list-inline-item"><a href="{{ path('auteur_show', {'id': auteur.id}) }}" class="text-reset">{{auteur}}</a></li>
                    {% endfor %}
                    .</ul>
                </p>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
</div>

{% endblock %}
